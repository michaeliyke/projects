pt( Init );
function Init( body, head, doc, _, $, w ) {
var slice = function (rr){
    return Array.prototype.slice.call(rr);
}

Techie(function(){

    var b = slice(pt("#ajaxDiv", "head"));
        a(b.length)
    });
    


}
